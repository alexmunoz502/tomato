import AppPage from "./AppPage";

export const SETTINGS_PAGE_ID = "settings";

const SettingsPage = () => {
  return <AppPage id={SETTINGS_PAGE_ID}>This is the settings page.</AppPage>;
};

export default SettingsPage;
